# legacy/

已停止维护的早期原型，保留供追溯，不参与构建。

- `content-v0/` — 早期 JSON 内容 schema 试验（`zz-test-*` 样例 + 双语 schema README）。
  该 schema 从未被现行站点采用；现行内容目录是根级 `content/`（Markdown + YAML frontmatter）。

根级 `src/` 下的 Next.js 原型同样已停止维护，现行站点在 `app/`。
