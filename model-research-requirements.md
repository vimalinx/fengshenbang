# 封神榜 · 模型详情页调研需求文档

> 版本：v1.0 · 2026-08
> 配套文件：`model-detail-checklist.md`（区块→字段对照表，边填边查）
> 完整样板：`app/src/data/modelDetails.ts` 的 `OPUS_5`（claude-opus-5），页面参照 `opus5-fullpage.png`
> 用途：给「调研一个模型、产出一个详情页」这项工作定需求——调研什么、去哪调研、要多少、要什么格式、怎么算合格。

---

## 0. 使用方式

1. 确定目标模型与**调研基准日**（所有热度数字、情绪判断以基准日为准，文档里注明）。
2. 按 §3–§8 六个阶段顺序采集，每阶段末尾有验收点。
3. 采集结果先落进 §9 的 `data.json` 模板，能填的填满，填不了的进 `uncertain` 清单——**不允许留空白但不留说明**。
4. 完成后过 §10 验收清单，全绿才交付给数据录入（转 `modelDetails.ts`）。

---

## 1. 调研对象与产出物

**输入**：一个目标模型（如 Claude Opus 5），及其上一代型号（对照基准）、2-3 个同档对手。

**产出**：

| 产出物 | 说明 |
|---|---|
| `data.json` | 按 §9 模板填写的结构化调研数据，是录入 `modelDetails.ts` 的唯一来源 |
| 调研笔记 | 自由格式，保留关键帖子的链接、截图、原文摘抄，支撑 `sources` 与日后复核 |
| `_sources` 链接池 | 调研过程中收集的全部链接（20-40 条），从中挑 6-10 条进 `sources` |

**一句话原则**：页面上每一个数字、每一句引文，都要能回答「这是从哪来的」。答不上来的，要么删掉，要么进 `uncertain`。

---

## 2. 总体原则与数量规格

### 2.1 四条铁律

1. **真实可追溯**：榜单成绩、价格、日期只信官方/权威机构；社区内容引用真实帖子，记录 URL + 赞数/浏览量快照。
2. **估算必须声明**：雷达评分、情绪比例、任何"大概""估计"的数，全部进 `uncertainties`。
3. **正负平衡**：锐评、弹幕、平台情绪不许一边倒。一个被吹爆的模型也要找到真实的负面声音（反之亦然）。
4. **时间口径统一**：热度数字（HN points、赞数、浏览量）会随时间涨，采集时记录基准日，同一次调研内的数字必须是同一时点。

### 2.2 数量规格总表（最低要求）

| 类别 | 字段 | 数量 | 详节 |
|---|---|---|---|
| 档案 | `profile` 七项 | 全填 | §3.1 |
| 历代版本 | `constellation` | 4-6 代 | §3.3 |
| 天赋 | `talents` | 3-4 个，各带硬指标 | §3.4 |
| 榜单成绩 | `benchGroups` 成绩组 | 5-8 行 | §4.1 |
| 规格参数 | `benchGroups` 规格组 | 7 行（固定项） | §3.2 |
| effort 档位 | `effortBench` | 3-5 个 benchmark × 档位（仅支持调节的模型） | §4.2 |
| 子榜单 | `subBoards` | 3-5 条 | §4.3 |
| 平台情绪 | `platforms` | 5-7 个平台 | §5.1 |
| 情绪比例 | `sentiment` | 3 数合计 100 | §5.2 |
| 强项/弱项 | `strengths` / `weaknesses` | 各 5 条 | §5.3 |
| 雷达 | `radar` | 固定 10 维，0-100 | §5.4 |
| 细分反馈 | `notes` | 3 段（编程/推理/中文） | §5.5 |
| 热度数字 | `heat` | 4 个 | §5.6 |
| 名家锐评 | `expertQuotes` | 18-22 条 | §5.7 |
| 短引用 | `quotes` | 5-8 条 | §5.8 |
| 弹幕 | `danmaku` | 12-16 条（main 5-6 条） | §5.9 |
| 大事记 | `timeline` | 4-8 条带日期 | §6.1 |
| 精进/失守 | `versionDelta` | 各 5-7 条 | §6.2 |
| 争议 | `controversies` | 2-4 对（事件+回应） | §6.3 |
| 升级共识 | `upgradeConsensus` + `consensusNote` | 1 结论 + 1 段话 | §6.4 |
| 实测落差 | `benchmarkGap` | 1 段话 | §6.5 |
| 官方演示 | `demos` | 3 条 | §6.6 |
| 擅长/不擅长 | `trialGood` / `trialBad` | 3-4 / 3-4 条 | §7.1 |
| 推荐装备 | `bestInSlot` + `harnessReviews` | 3 个装备，各 1 条点评 | §7.2 |
| 对手 | `rivalIds` | 3 个 | §7.3 |
| 配队/攻略 | `teamIds` / `guideIds` | 2 / 3-4 个（从已有库关联） | §7.4 |
| 信源 | `sources` | 6-10 条 | §8.1 |
| 存疑 | `uncertainties` | 3-6 条 | §8.2 |

---

## 3. 阶段 A：官方硬数据

> 来源优先级：官方发布博客/公告 > 官方文档/定价页 > 官方技术报告 > 权威媒体（Reuters/The Verge/机器之心）转述。**这一阶段不允许出现"据说"。**

### 3.1 档案 `profile`

| 字段 | 要求 | 去哪查 |
|---|---|---|
| `apiId` | API 调用的精确模型标识（如 `claude-opus-5`） | 官方 API 文档 |
| `vendor` | 发布方全称 | — |
| `releaseDate` | 精确到日 `YYYY-MM-DD` | 官方发布博文日期 |
| `access` | 获取方式数组：API / 订阅档位 / 开源 / 内置于某 Harness | 官方定价页、订阅页 |
| `costNote` | 接入成本一句话：入/出价 + 与上代/对手的价格关系 + 长上下文是否加价 | 官方定价页，算一笔对比账 |
| `nicknames` | 2-3 个社区诨名 | 阶段 C 采集时顺手收集，官方名不算诨名 |
| `signature` | 招牌成就一句，带数字（如「ARC-AGI-3 30.16% · 纪录约为第二名 4 倍」） | 从 §4 榜单数据里挑最炸的一条 |

### 3.2 规格组 `benchGroups` 末组（固定 7 行）

页面头版速览栏直接渲染，行序固定：

1. 上下文窗口（如 `1,000,000 tok`）
2. 最大输出
3. 价格（入/出）（格式 `$5 / $25 · Mtok`）
4. effort 档位（无则写「—」或该模型的等价调节项）
5. 模型架构（官方说法；未公开则写官方披露的最接近表述，推测部分进 `uncertainties`）
6. 发布日期
7. 获取方式

**验收点**：每一行都能指到官方页面；架构、effort 两栏最容易出错，重点复核。

### 3.3 历代版本 `constellation`

- 4-6 代：`version`（版本号）+ `date`（`YYYY-MM`）+ `effect`（该代一句话成就，游戏化措辞，≤20 字）+ 当前代标 `current: true`。
- 只列同一产品线（Opus 系列列 Opus，不混 Sonnet）。
- 成就写法参照样板：「上下文 1M · 续航 40h · 登临 T0」——2-3 个要点用 `·` 连。

### 3.4 天赋 `talents`

3-4 个核心能力，每个含：

- `kind`：`normal`（常态特性）/ `skill`（主动技）/ `burst`（大招/最亮点）/ `passive`（被动），至少一个 burst；
- `seal`：单字印（思/程/破/验/御/器/审……选贴合的字）；
- `name` + `desc`：能力名 + 一两句人话描述；
- `metric`：**必须有硬指标一行**（如「Frontier-Bench 43.3% · 全场最高」）。没有数字支撑的能力不算天赋，删掉或降级。

### 3.5 官方演示 `demos`（放这里一起采）

官方发布会/博文里的演示亮点 3 条：`title`（4-8 字）+ `desc`（一两句，带数字）。官方演示不足 3 条时，缺的标 `placeholder: true` 并在笔记里说明。

**阶段 A 验收**：profile 七项齐、规格 7 行齐、constellation ≥4 代、talents ≥3 且个个有 metric、demos 3 条。

---

## 4. 阶段 B：榜单与对战数据

### 4.1 榜单成绩 `benchGroups` 成绩组（5-8 行）

- 必查榜（有则必收）：SWE-bench Verified、SWE-bench Pro、LMArena（综合/编程）、ARC-AGI 系列、Aider Polyglot、Terminal-Bench、GDPval、BenchLM 类综合榜。
- 每行：`label`（榜单名）+ `value`（成绩，含 `#1` / `纪录` 标记——页面会对这两个词朱砂高亮）。
- 来源：官方发布数据 + 榜单官网（swebench.com、lmarena.ai、arcprize.org 等）交叉核对；**官方自报与第三方复测不一致时，以第三方为准并在笔记记录差异**。

### 4.2 effort 档位矩阵 `effortBench`（仅支持 effort/思考预算调节的模型）

- 最难采的一块。要的是**同一 benchmark 在不同档位下分别的成绩**。
- 来源：官方技术报告/系统卡 > 第三方复测（如 ARC Prize 分档测试）> 社区自测帖（只能用带方法论说明的）。
- 未测的组合填 `null`，用 `note` 注明原因（如「官方未披露档位」「测试窗口短仅评 High」）。
- 至少 3 个 benchmark 有分档数据才渲染此表，否则整个 `effortBench` 缺省并在 `uncertainties` 说明。

### 4.3 子榜单交叉 `subBoards`（3-5 条）

细分领域排名：WebDev 榜、Agent Arena、编程榜、长文写作榜等。每条 `name` + `rank`（#1 / 前 5 占 4 席 / High 档超 XX）+ `note`（补充：从谁手里夺的、置信区间等）。

**阶段 B 验收**：成绩组 ≥5 行、关键榜单与官网核对过、effortBench 要么够 3 行要么明确缺省、subBoards ≥3。

---

## 5. 阶段 C：社区体感（工作量最大，预留 60% 时间）

> 覆盖平台：Reddit、HackerNews、知乎、Linux.do、V2EX、掘金、X。
> 通用搜索词：`{模型名}`、`{模型名} review`、`{模型名} vs {对手}`、`{模型名} 体验/实测/怎么样`、诨名。
> 时间窗：发布后两周内为主，另搜「一个月后」「长期使用」类回头评价。

### 5.1 分平台情绪 `platforms`（5-7 行）

每个平台一行：`name` + `tone`（pos/mix/neg）+ `summary`（一两句，**必须带具体数字或具体帖子**——赞数、points、评论数、浏览量、帖子标题）。

各平台采集手册：

| 平台 | 怎么搜 | 记什么 |
|---|---|---|
| Reddit | r/ClaudeAI、r/LocalLLaMA、r/singularity 及对应厂商 sub；按 top/week 排序 | 热帖标题、赞数、评论数、主流观点倾向、代表性楼层 |
| HackerNews | 发布帖（通常最大楼）+ 后续讨论帖 | 发布帖 points 与评论数、高亮评论观点、质疑点 |
| 知乎 | 「如何评价 {模型}」类问题 | 问题浏览量、回答数、高赞回答共识 |
| Linux.do | 站内搜索模型名，看评测帖/横评帖/暴论帖 | 评测结论、楼层共识、暴论（反向声音） |
| V2EX | 站内搜索 | 讨论质量判断（注意中转站广告噪音，噪音本身也值得写进 summary） |
| 掘金 | 模型名 + 实测/体验 | 技术专栏观点、机制研究向文章（/doctor 类） |
| X | 官方公告转评、KOL（Swyx、Claire Vo 类）、对战榜官方号 | KOL 原话、盲测/对战结果、典型心态概括 |

tone 判定：多数帖/高赞观点正面 → pos；明显两派且都有分量 → mix；主流吐槽 → neg。**不许全 pos**——如果一个平台找不到负面，在笔记里写明「未找到有分量的负面声音」。

### 5.2 整体情绪比例 `sentiment`

`positive` + `mixed` + `negative` = 100。跨平台加权估：发布热度高的平台（HN/Reddit）权重大。这是估算值——**固定进 `uncertainties`**（样板原文：「各平台情绪比例为代表性帖文估算，非严格量化」）。

### 5.3 公认强项/弱项 `strengths` / `weaknesses`

- 各 5 条短词（2-8 字）：从 §5.1 的 summary 里提炼出现频率最高的评价。
- 强项弱项要和 `notes`、`radar` 自洽：弱项里有「速度慢」，radar 的响应速度就不能给 80。

### 5.4 体感雷达 `radar`（固定 10 维，0-100）

长程任务 / 编程工程 / 抽象推理 / 上下文利用 / 中文能力 / 响应速度 / 稳定性 / 指令遵循 / 易用性 / 性价比。

- 顺序固定，维度固定，一个不能少。
- 打分依据：社区讨论（主）+ 榜单成绩（辅）。这是体感分不是跑分——估算属性进 `uncertainties`。
- 自洽检查：与 strengths/weaknesses、sentiment、verdict 互相不打架（如整体负面 35% 的模型不该出现全 90+ 的雷达）。

### 5.5 细分反馈 `notes`（3 段）

`编程` / `推理` / `中文` 各一段（3-5 句）：先说共识，再说分歧，引用具体帖子或数字收尾。中文段如果反馈确实少，照实写「专项反馈较少，无明显负面」。

### 5.6 讨论热度 `heat`（4 个硬数字）

固定四项：HN 发布帖 points、HN 评论数、Reddit 最高赞、知乎浏览量。数值带基准日快照（笔记里记）。

### 5.7 名家锐评 `expertQuotes`（18-22 条，质量关键区）

- 每条：`text`（**原话**，不改写）+ `name`（署名）+ `role`（身份/出处，如「Cursor 联合创始人」「Reddit · 486 赞帖」）+ `tone`（pos/mix/neg）。
- 构成要求：
  - 中英文都要有（大致 6:4）；
  - pos / neg / mix 都要有，比例跟着实际舆情走，但 neg 不少于 1/4；
  - 来源分层：官方/机构发言 2-3 条 + 行业名人/KOL 3-5 条 + 媒体/Newsletter 1-3 条 + 社区热帖楼主/高赞 8-12 条；
  - 长句保留原汁原味（英文不翻译），社区匿名用户署名用「u/ 热帖楼主」这类描述性称呼。
- 与 `danmaku`/`quotes` 可以同源，但 expertQuotes 要**有头有脸**——匿名的碎碎念去弹幕，不进锐评。

### 5.8 短引用 `quotes`（5-8 条）

`text`（一句）+ `source`（平台+身份）+ `tone`（pos/neg）。从 expertQuotes 候选里挑最短的，或单独收集的短金句。

### 5.9 背景弹幕 `danmaku`（12-16 条）

- `text` ≤ 30 字为宜；`platform` ∈ reddit/hn/x/zhihu/linuxdo/v2ex（`bilibili` 可选）；`main` 5-6 条。
- main 大胶囊 = 主流/高赞观点（「发布帖 1378 points：能力令人惊叹」这种有分量的）；小号弹幕放补充性、趣味性声音。
- 正负都要有，平台至少覆盖 4 个。

**阶段 C 验收**：7 平台行齐且行行带数字、sentiment 合计 100、强弱项各 5、radar 10 维、notes 3 段、heat 4 项、expertQuotes 18-22 且构成达标、quotes ≥5、danmaku 12-16 且 main 5-6。

---

## 6. 阶段 D：变迁、争议与共识

### 6.1 发布大事记 `timeline`（4-8 条）

`date`（MM-DD）+ `event`（一句）。按时间序收录：正式发布 → 独立验证/复测 → 事故/故障 → 媒体曝光 → 登顶/里程碑 → 官方修复。只收有明确日期的事件。

### 6.2 精进/失守 `versionDelta`

- `base`：上一代型号全名。
- `improves` / `regresses`：各 5-7 条，**每条尽量带前后数字对比**（「SWE-bench Verified 88.6% → 96.5%」）。退步条数字找不到就写行为变化（「安全护栏比上代更严，影响日常使用」）。
- 来源：官方 changelog/发布文对比 + 社区「用回旧版」类讨论。

### 6.3 争议事件 `controversies`（2-4 对）

`event`（事件：时间+发生了什么+规模数字）+ `response`（官方回应原文要点；官方没回应就缺省并在笔记注明「官方未回应」）。

### 6.4 升级共识 `upgradeConsensus`

- 三选一：`worth`（值得升）/ `wait`（建议观望）/ `split`（社区分化）。
- `consensusNote`：一段话讲清什么场景该升、什么场景该留，分化模型的分歧点要写明。

### 6.5 实测落差 `benchmarkGap`

一段话（3-4 句）：榜单表现 vs 日常体感的矛盾 + 根因 + 社区给出的缓解方案。没有明显落差的模型也要写——写「分数与体感基本一致」的结论及依据。

### 6.6 官方演示 `demos`

见 §3.5，放这里复核一遍与 timeline 不重复。

**阶段 D 验收**：timeline ≥4 条全带日期、versionDelta 各 ≥5 条且 improves 半数以上带前后数字、controversies ≥2 对、consensus 有结论有话术、benchmarkGap 成文。

---

## 7. 阶段 E：场景、装备与外围（半调研半拍板）

### 7.1 擅长/不擅长 `trialGood` / `trialBad`

- 各 3-4 条，`label` **用大白话**（「长程代码重构」✓，「Agentic 长程任务」✗）；`to` 挂站内场景锚点（`/scenarios#xxx`，锚点清单见 `Scenarios.tsx`）。
- `trialBad` 每条必须带 `note` 换人建议（「速度慢，建议换 Gemini 3 Flash」）——推荐替代者须在 `models.ts` 中存在。
- 依据：§5 社区反馈 + §4 榜单结构，不许纯拍脑袋。

### 7.2 推荐装备 `bestInSlot` + `harnessReviews`（3 个）

- `bestInSlot`：`id` 须在 `harnesses.ts` 存在；`note` 写细——为什么适配（同宗/官方联动）、有什么实测背书（数字）、怎么搭配用（档位/模式建议）。
- `harnessReviews`：每个装备一条实测点评；某装备确实没实测数据时，写占位文并标 `placeholder: true`。
- 首选三坑：该模型本命 Harness（同厂商 CLI/IDE）+ Cursor + OpenHands；有更强的社区证据时可替换。

### 7.3 对手 `rivalIds`（3 个）

雷达对比用。同价位/同定位 2 个 + 上一代 1 个；id 必须在 `models.ts` 存在。

### 7.4 配队与攻略 `teamIds` / `guideIds`

从 `teams.ts` / `guides.ts` 现有库里关联（2 个队、3-4 篇攻略）。库里没有合适的？在笔记里列出缺口，交给内容排期，不要为此新造数据。

### 7.5 头版文案（拍板项，不写进 data.json，录入时写）

- `title`（封号，4-8 字，如「推理破局旗舰」）、`verdict`（一句判词，点出核心矛盾，首字符不能是标点——首字下沉用）、`tier` / `stars` / `tags`（≤3 个）、`composite`（综合战力，参考 radar 均值与榜单位置拍）。
- 这些入 `models.ts`，风格对齐同档已有模型。

---

## 8. 阶段 F：信源与存疑

### 8.1 参考来源 `sources`（6-10 条）

从 `_sources` 链接池里挑：`title` + `platform` + `url`。构成：官方发布 1-2 + 权威榜单/机构 1-2 + 最大社区帖 2-3 + 深度评测 1-2 + 争议报道 0-2。死链检查一遍。

### 8.2 存疑标注 `uncertainties`（3-6 条）

诚实清单。固定候选：

- 架构/参数量未公开（如适用）；
- 情绪比例与雷达为估算（如适用，基本总是适用）；
- 精确 ELO/分档成绩未找到（如适用）；
- 内部测试事件未在外部复现（如适用）。

写法和事实区分清楚：「未找到」「为推测」「非严格量化」是关键词。

---

## 9. 交付物模板：`data.json`

> 字段名对齐调研→页面映射（见 `model-detail-checklist.md` 末尾速查）。以下为完整骨架，`//` 注释为填写要求，正式交付时去掉注释。

```json
{
  "model": {
    "系列": "claude",
    "版本号": "Opus 5",
    "发布日期": "2026-07-24",
    "发布方": "Anthropic",
    "API标识": "claude-opus-5",
    "定价": "$5 / $25 · Mtok（与 Opus 4.8 同价，为 Fable 5 一半）",
    "上下文窗口": "1,000,000 tok",
    "最大输出": "128,000 tok",
    "effort档位": "Low / Medium / High / Max",
    "模型架构": "Hybrid Reasoning（参数量未公开）",
    "获取方式": ["API", "Claude Code 内置", "Max 订阅默认", "Pro 可用"]
  },
  "诨名": ["ADHD 模型", "O5", "小 Fable"],
  "招牌成就": "ARC-AGI-3 30.16% · 纪录约为第二名 4 倍",

  "榜单表现": [
    { "name": "SWE-bench Verified", "value": "96.5% · #1", "source": "url" }
  ],
  "effort分档成绩": [
    { "name": "ARC-AGI-1", "Low": null, "Medium": null, "High": 97.5, "Max": 97.5, "note": "" }
  ],
  "子榜单交叉": [
    { "name": "WebDev 榜", "rank": "#1", "note": "从 Kimi K3 手中夺得" }
  ],
  "官方演示": [
    { "title": "自写 CV 流水线", "desc": "……" }
  ],

  "相比上一版进步": ["SWE-bench Verified 88.6% → 96.5%，登顶榜首"],
  "相比上一版退步": ["简单任务过度思考，被称「ADHD 模型」"],
  "社区升级共识": "split：长程 agentic 编程值得升级；简单任务建议 medium effort 或留在 Opus 4.6",

  "公认强项": ["长程 Agentic 任务", "编程工程", "抽象推理", "1M 全仓审查", "性价比"],
  "公认弱项": ["过度思考", "过度主动", "安全护栏过严", "速度慢输出长", "需调教配置"],
  "编程能力反馈": "两极分化：……",
  "推理能力反馈": "核心卖点：……",
  "中文能力反馈": "……",
  "十维体感评分": {
    "长程任务": 96, "编程工程": 90, "抽象推理": 98, "上下文利用": 88, "中文能力": 72,
    "响应速度": 38, "稳定性": 45, "指令遵循": 42, "易用性": 40, "性价比": 80
  },

  "整体情绪倾向": { "positive": 45, "mixed": 20, "negative": 35 },
  "Reddit情绪": "neg · 高度分化：……（RL-fried 帖 110 赞 98 评）",
  "HackerNews情绪": "pos · 发布帖 1378 points / 746 评论……",
  "中文社区情绪": {
    "知乎": "pos · 浏览 64 万+……",
    "Linux.do": "mix · ……",
    "V2EX": "mix · ……",
    "掘金": "pos · ……"
  },
  "Twitter-X情绪": "pos · 官方与 KOL 盛赞；典型心态 brilliant but annoying",
  "讨论热度": { "HN points": "1,378", "HN 评论": "746", "Reddit 最高赞": "486", "知乎浏览": "64 万+" },

  "正面代表评价": [
    { "text": "Literally the BEST at long-horizon task.", "name": "u/ 热帖楼主", "role": "Reddit · 486 赞帖", "url": "" }
  ],
  "负面代表评价": [
    { "text": "It's an 'ADHD model'……", "name": "Reddit 平衡讨论帖", "role": "r/ClaudeAI", "url": "" }
  ],

  "主要争议": [
    { "event": "沙箱逃逸（7/30 曝光）：……", "官方回应": "商业版安全护栏可拦截……" }
  ],
  "发布大事记": [
    { "date": "07-24", "event": "正式发布：……" }
  ],

  "推荐场景": ["长程代码重构", "Agent 开发", "算法与抽象推理"],
  "不推荐场景": [
    { "label": "前端快速出活", "建议换": "Gemini 3 Flash", "原因": "速度慢" }
  ],
  "装备实测": {
    "claude-code": "本命装备。/doctor 精简后体感大幅改善……",
    "cursor": "Cursor 联创：近 Fable 5 智能……",
    "openhands": "（无数据，占位）"
  },
  "对手": ["gpt-5-2", "claude-opus-4-7", "gemini-3-pro"],

  "uncertain": [
    "模型架构未公开：参数量与 MoE/稠密细节为推测",
    "各平台情绪比例为代表性帖文估算，非严格量化"
  ],
  "_sources": [
    { "title": "", "platform": "", "url": "" }
  ],

  "_meta": {
    "调研基准日": "2026-08-08",
    "调研人": "",
    "覆盖平台": ["reddit", "hn", "zhihu", "linuxdo", "v2ex", "juejin", "x"]
  }
}
```

---

## 10. 验收清单（Definition of Done）

**完整性**

- [ ] §2.2 数量规格总表逐项达标（数量、格式）
- [ ] `data.json` 无空白字段；填不了的全在 `uncertain` 有对应条目
- [ ] `_meta.调研基准日` 已填，所有热度数字为该时点快照

**真实性**

- [ ] 官方数据（价格/日期/规格/榜单）与官方页面逐一核对过
- [ ] 每条锐评、弹幕是真实原话，笔记里有出处链接
- [ ] 估算类数据（radar、sentiment）已进入 `uncertain`

**平衡与自洽**

- [ ] 锐评含 neg ≥ 1/4，中英混合；弹幕平台覆盖 ≥ 4
- [ ] 平台情绪非全 pos（或笔记说明理由）
- [ ] radar / strengths / weaknesses / sentiment / verdict 互相不打架
- [ ] trialBad 每条有换人建议，替代者在 `models.ts` 存在

**可录入**

- [ ] 字段名与 §9 模板一致，录入 `modelDetails.ts` 时不需要再猜结构
- [ ] `rivalIds` / `bestInSlot.id` / `teamIds` / `guideIds` 均存在于对应数据文件
- [ ] `sources` 6-10 条，逐条点开验证非死链

---

## 附录 A：字段 → 页面区块速查

调研字段到页面区块、`modelDetails.ts` 字段的三方对照，见 `model-detail-checklist.md` 全文（#1-#30 + 外围数据 + 对照速查三节）。本文档负责「怎么调研」，checklist 负责「填到哪去」。

## 附录 B：常见坑

1. **官方自报 vs 第三方复测**：成绩组优先第三方；官方数据用于规格组。
2. **V2EX/贴吧类噪音**：中转站广告泛滥时，噪音本身就是 summary 的素材（样板就是这么写的）。
3. **诨名收集**：从发布初期帖子的标题和高赞评论里找，官方 marketing 词不算。
4. **情绪比例别装精确**：45/20/35 这种看起来有零有整的数，本质是估计，必须进 uncertain。
5. **上一代数据复用**：versionDelta 的「进步」列里，上代成绩可直接从上个模型的 data.json 抄，不用重查。
6. **时间敏感**：发布不满两周的模型，timeline/heat 会快速过时，录入后 2-4 周安排一次复查。
